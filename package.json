{
  "name": "learnexpress",
  "version": "1.0.0",
  "description": "Learning to expressjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "npm-run-all -p server:*",
    "server:start": "node server.mjs",
    "server:watch": "npx tailwindcss -i ./app/style.css -o ./app/dist/output.css --watch"
  },
  "author": "yuce",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.3.1"
  }
}
